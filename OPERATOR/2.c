//2

#include <stdio.h>

int main ()
{
    int a;
    float b;
    float x=(3.31 * a*a + 2.01 * b*b*b
) / (7.16 * b*b + 2.01 * a*a*a
);
    printf ("a,b=");
    scanf("%d%f",&a,&b);
    printf("x=%f",x);
    return 0 ;

    }
